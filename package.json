{
  "name": "orb-research-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Historical research and backtesting platform for Opening Range Breakout (ORB) strategies.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test \"tests/**/*.test.js\"",
    "verify": "sh scripts/verify-foundation.sh",
    "check": "npm run verify && npm test"
  }
}
